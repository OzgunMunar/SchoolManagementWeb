@page
@model ForgotPasswordModel
@{
    ViewData["Title"] = "Forgot your password?";
}

<div class="col-lg-8 col-md-6 container mt-3">
    <div class="card shadow-sm">
        <div class="card-body">
            <div class="d-flex justify-content-center align-items-center flex-column">
                <h1>@ViewData["Title"]</h1>
                <h6 class="font-monospace break-word">Easy! We'll mail you instructions to reset your password. <br />If you don't have access to your email anymore, you can reach us out by phone or email.</h6>
                <hr />
                <form method="post" class="w-25 mt-3">
                    <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                    <div class="form-floating mb-3">
                        <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                        <label asp-for="Input.Email" class="form-label"></label>
                        <span asp-validation-for="Input.Email" class="text-danger"></span>
                    </div>
                    <button type="submit" class="w-100 btn btn-lg rounded-0 softblue-bgcolor text-white">
                        <span>Reset Password</span>
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
